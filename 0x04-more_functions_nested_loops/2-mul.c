#include "holberton.h"

/**
 * mul - checks if c is lowercase
 * @a: The character to print
 * @b: The character to print
 *
 * Return: On success 1.
 * On error, 0 is returned, and errno is set appropriately.
 */
int mul(int a, int b)
{
int c;
c = a * b;
return (c);
}
