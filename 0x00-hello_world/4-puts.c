#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char quote[] = "\"Programming is like building a multilingual puzzle";

puts(quote);

return (0);
}
